## Environment

You are running inside the following environment. Treat this as ground truth for the current session; you do not need to re-discover these facts with tools.

- Working directory: `{{cwd}}`
- Platform: {{os}}/{{arch}}
- Today's date: {{date}}
- Git: {{git_branch}}{{git_dirty}}
- Detected project type: {{project_type}}

### Top-level layout

```
{{tree}}
```

If anything above looks stale (e.g. you create files / switch branches), re-check it with the appropriate tool (`git_status`, `tree`, `ls`).
